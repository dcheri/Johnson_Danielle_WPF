// Danielle Johnson | November 21, 2013 | Functions_Industry


var finalPrice = posterPrice (prompt("What is the original price of the item? \($xx.xx\)"), prompt("What is the percentage off the original price of the item? \(XX\))")); //Defines the black Friday price variable and gets the users price and discount amount for the calculation
	function dealFinder(originalPrice, percentDiscount) { //Tells the function called dealFinder that we'll be passing two variables to it
		




console.log("The sale price of your item is $" + Number(posterPrice) + "."); //Prints the result to the console

var priceDiscount = function (posterPrice, discount){ //The discount price calculation
	var extraSavings = posterPrice - (posterPrice * ( Number(discount)/100));
	return extraSavings; //Returns the value
}